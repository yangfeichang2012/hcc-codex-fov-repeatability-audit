# Python's CSV writer emits RFC-style CRLF records on every platform.
# Preserve the frozen TSV bytes so clean Linux runners compare like-for-like.
expected/*.tsv -text
expected/*.json text eol=lf
expected/SHA256SUMS.txt text eol=lf
